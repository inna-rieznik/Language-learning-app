/*
import s from "./Button.module.css";
import {NavLink} from "react-router-dom";

const Button = (props) => {
    let path = props.urlName;
    return (
        <div>
            <button className={`${s.primary} ${s.button}`}>
              <NavLink to={path}>{props.buttonAction}</NavLink>
            </button>
        </div>
    );
}

export default Button;*/
